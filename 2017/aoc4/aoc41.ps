#!/usr/local/bin/pwsh
$v = $Input | Where-Object {
    $words = $_.Split(" ")
    $wcount = $words.Length
    $uniq = $words | sort | uniq
    $ucount = $uniq.Length

    $wcount -eq $ucount
} | Measure-Object

$v.Count